export const performanceProfile = {
  goal: 'Keep the visual quality and layout unchanged while reducing animation cost, canvas work, and unnecessary pointer work.',
  strategy: ['adaptive particle density', '30fps canvas cap', 'mobile synapse reduction', 'reduced-motion support', 'touch-device cursor skip'],
};
